<?php
// session_start();
// if(isset($_SESSION['l_uname'])){
    // Set a cookie based on the 'l_uname' session variable
    // $cookie_name = 'username';
    // $cookie_value = $_SESSION['l_uname'];
    
    // Set the cookie to expire in, for example, 1 hour (3600 seconds)
    // $cookie_expire = time() + 3600;
    
    // Set the cookie for the entire domain
    // $cookie_path = '/';
    
    // Set the cookie
    // setcookie($cookie_name, $cookie_value, $cookie_expire, $cookie_path);
    

//     echo "Cookie 'username' is set with the value: " . $_SESSION['l_uname'];
// } else {
//     echo "<script>alert('No user record found, Please Login First')</script>";
//     echo "<script>location.href='../Home/LandingPage.html'</script>";
// }
?>
